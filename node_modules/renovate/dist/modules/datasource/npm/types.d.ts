import { HostRule } from "../../../types/host-rules.js";
import { PackageRule } from "../../../config/types.js";

//#region lib/modules/datasource/npm/types.d.ts
interface NpmrcRules {
  hostRules: HostRule[];
  packageRules: PackageRule[];
}
interface NpmAttestations {
  url?: string;
}
interface NpmDistribution {
  attestations?: NpmAttestations;
}
interface NpmResponseVersion {
  repository?: {
    url: string;
    directory: string;
  };
  homepage?: string;
  deprecated?: boolean;
  gitHead?: string;
  dependencies?: Record<string, string>;
  devDependencies?: Record<string, string>;
  engines?: Record<string, string>;
  dist?: NpmDistribution;
}
interface NpmResponse {
  _id: string;
  name: string;
  versions?: Record<string, NpmResponseVersion>;
  repository?: {
    url?: string;
    directory?: string;
  };
  homepage?: string;
  time?: Record<string, string>;
  'dist-tags'?: Record<string, string>;
}
type Npmrc = Record<string, any>;
//#endregion
export { NpmAttestations, NpmDistribution, NpmResponse, NpmResponseVersion, Npmrc, NpmrcRules };
//# sourceMappingURL=types.d.ts.map